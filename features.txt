1. Button — wire a physical button to reset the counter. Teaches digital input, digitalRead(), and you'll immediately feel why millis() beats delay()
2. Animation — a dot rotating around the circle using sin()/cos(). Teaches trigonometry in code and looks great on a round display
3. Structs — the C++ way to group related data (like a Java class but simpler). Useful before things get more complex